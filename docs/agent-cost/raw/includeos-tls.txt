
IncludeOS supports TLS. The lwn.net article "IncludeOS: a unikernel for C++ applications" from July 25, 2017, states: "The dev branch of IncludeOS does support SMP. For an upcoming delivery we need to do TLS at scale and we're using SMP to handle a large amount ...". However, the specific library (e.g., mbedTLS, OpenSSL, BearSSL) used for TLS support is not mentioned in the sources I have read.

confidence: medium
unresolved: The specific TLS library used by IncludeOS.
steps: 5 of 12, urls retrieved: 10
COST llm_calls=5 tokens_in=10876 tokens_out=370 searches=2 fetches=1 steps=5

sources:
  [1] https://lwn.net/Articles/728682/
